import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ng-oauth2-oidc-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
